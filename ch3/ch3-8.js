'use strict'

const printOut = (...str) => {
    return str
}

console.log(printOut("魁!!男塾", "聖闘士星矢"))
console.log(printOut("魁!!男塾"))

// 「..strというrestパラメータに、文字列引数を1個渡したら、何が起きるだろうか」の動作確認
// 特に結果を利用できるような有益な例を思い付かなかった => TODO 何かしら有益な例を見つける！
